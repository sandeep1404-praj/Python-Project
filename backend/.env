# Library Manager Database Configuration
# ==========================================

# Database Type
# Set to 1 to use SQLite (db.sqlite3)
# Set to 0 to use MySQL
USE_SQLITE=0

# MySQL Database Settings
# Update these values based on your MySQL installation

# Database name (will be created if it doesn't exist)
DB_NAME=library_manager

# MySQL username (default: root)
DB_USER=root

# MySQL password (set during MySQL installation)
# For Docker: root
DB_PASSWORD=Sandeep@1404

# MySQL host/IP address
# 127.0.0.1 = localhost (your computer)
# Use other IP for remote MySQL server
DB_HOST=127.0.0.1

# MySQL port (default: 3306)
DB_PORT=3306

# ==========================================
# IMPORTANT NOTES:
# 
# 1. Make sure MySQL is running before running setup_mysql.py
#    - Docker: Run start-mysql.bat or start-mysql.ps1
#    - Native: Start MySQL service from Windows Services
#
# 2. If using Docker, run this first:
#    start-mysql.bat
#
# 3. Then run the setup script:
#    python setup_mysql.py
#
# 4. To go back to SQLite:
#    Change USE_SQLITE=1
#    Restart Django server
#
# ==========================================
